// Task 2.1 Program take coordinates of two dots and must find a distance between them

/*
double distance(int x1, int y1, int x2, int y2)
{
    double distX12 = x2 - x1;
    double distY12 = y2 - y1;
    double distBetween2Points = Math.Sqrt(distX12 * distX12 + distY12 * distY12);
    return distBetween2Points;
}

Console.Write("Input X coordinate of A point --> ");
int xA = Convert.ToInt32(Console.ReadLine());
Console.Write("Input Y coordinate of A point --> ");
int yA = Convert.ToInt32(Console.ReadLine());

Console.Write("Input X coordinate of B point --> ");
int xB = Convert.ToInt32(Console.ReadLine());
Console.Write("Input Y coordinate of B point --> ");
int yB = Convert.ToInt32(Console.ReadLine());

double distanceAB = distance(xA, yA, xB, yB);
Console.Write("Distance between points is --> " + distanceAB);
*/

// Task 2.2 Program take a number N>0 and then show squares of numbers from 1 to N

/*
void showSquareToN(int n)
{
    int count = 1;
    while (count <= n)
    {
        Console.Write(count * count + "; ");
        count++;
    }
}

Console.Write("Input some natural number --> ");
int num = Convert.ToInt32(Console.ReadLine());
showSquareToN(num);
*/